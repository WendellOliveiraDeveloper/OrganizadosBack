spring:
  application:
    name: Organizados

  datasource:
    url: jdbc:mysql://localhost:3306/organizados_db?useSSL=false&serverTimezone=UTC
    username: root
    password: Gold20050404!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect